{
  "name": "olrd-streams",
  "version": "1.0.0",
  "private": true,
  "description": "OLRD STREAMERS — build step obfuscates the site JS for deployment",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "javascript-obfuscator": "^4.1.1"
  }
}
